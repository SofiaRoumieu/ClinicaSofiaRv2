import { AgrandarGraficoDirective } from './agrandar-grafico.directive';

describe('AgrandarGraficoDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarGraficoDirective();
    expect(directive).toBeTruthy();
  });
});
